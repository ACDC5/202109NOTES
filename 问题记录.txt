npm:
	问题:
		1、npm install的时候出现unexpected end of file错误提示时的解决办法
			运行npm cache clean --force 清空npm缓存即可决这个问题 



		2、
		包名写错了，npm在注册表(https://registry.npmjs.org/)中找不到包
		npm ERR! 404
		npm ERR! 404  'reacct-native-safe-area-context@^0.7.2' is not in the npm registry.
		npm ERR! 404 You should bug the author to publish it (or use the name yourself!)
		npm ERR! 404 It was specified as a dependency of 'txclass'
		npm ERR! 404
		npm ERR! 404 Note that you can also install from a
		npm ERR! 404 tarball, folder, http url, or git url.





RN:
	{fieldData.map((item) => {
            return this.renderTitle(item) //数组map方法的回调函数必须是用return，否则renderTitle中的组件没法返回显式在页面中(无法拿到数据)
      })}


      <Image
	        source={uri:'http//:www.1111.com'} //使用uri而不是url
	    />



	  ----------------------------------------------------------------
	  
	  项目一直运行的很好突然报一堆砸了的错误，或者代码回退之后或重启项目后也报错的话(非语法上的):比如下面这样的:
	  	运行npx react-native run-android报错(没有修改项目的任何配置文件，显然这不是语法或代码上的问题):

	  	info Installing the app...
		> Task :app:checkDebugAarMetadata FAILED

		解决: 进入android目录执行gradlew clean 命令清除缓存;
